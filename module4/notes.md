Module 4 - Version cotrol
1. Git is a local tool for offline versioning and gitbuh is a website that hosts all repositories, accessible and sharable.
2. Git provies feature branch provision for parallel development work. The merged version has two parent commits, each from origin and feature branch.
3. github need personal tokens for auth, can generate tokens valid for 90 days or use github cli for auth.
4. git has 3 stages: working directory, stage and snapshot.
5. git push will actually update the online repository. Unpushed commits can be lost locally.